#ifndef _SORT_LIBRARY_H
#define _SORT_LIBRARY_H

#include <stdlib.h>
#include <limits.h>
#include <stdio.h>

void print_array(const int *array, size_t size);
void radix_sort(int *array, size_t size);

#endif /* _SORT_LIBRARY_H */
